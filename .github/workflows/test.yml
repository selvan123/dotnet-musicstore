name: Test Workflow

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name : list files
        shell : bash
        run  : |
          pwd
          echo "hello"
